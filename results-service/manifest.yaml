---
applications:
  - name: results-service
    memory: 1G
    services:
      - config-server
      - service-registry
      - rabbit
      - postgres
    path: target/results-service-1.0.0-SNAPSHOT.jar
    env:
      BP_JVM_VERSION: 25
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 25.+ } }'
